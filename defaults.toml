###################################################################
#   DO NOT EDIT THIS FILE UNLESS YOU KNOW WHAT YOU'RE DOING
#
#   If you wish to change settings, add the setting under the
#   same table and key name in 
#                   
#                   user-config.toml
#
#   If user-config.toml does not exist, you can create it in
#   project's root directory or specify a different config file
#   with the -config <filename> option
#
#   This file (defaults.toml) defines a default configuration 
#   for the Michgain Climbing Club Carpool scheduler
#   application.
#
###################################################################


title = "MCC Carpool Scheduler Configuration"

[mcc]

days_enabled = ["TUESDAY", "THURSDAY", "SUNDAY"]

[gspread]

    [gspread.responses.columns]

    email = 1
    name = 2
    phone_number = 3
    is_rider = 4
    is_driver = 5

    # the output sheet headings should follow the following format:
    # | rider_locs_day_1 |...| rider_locs_day_n | rider_times_day_1 |...| rider_times_day_n | (cont on next line)
    #           driver_locs_day_1 |...| driver_locs_day_n | driver_times_day_1 |...| driver_times_day_n
    #
    # this setting should mark the column index of rider_locs_day_1
    days_info_start = 8

    notes_column = 9

    # unused
    timestamp = 0


    [gspread.files]
    responses_sheet = "Carpool Form v20.0.0 (Responses)"
    dues_sheet = "dues-testing-v20.0.0"
    output_sheet = "Carpool test output 1"
    output_folder_id = "1j1w_0k5bIgqxJfmQmxbZZoGr66fJT4Y4"

    [gspread.output]

    # vertical spacing (in rows) between cars on the output sheet
    car_block_spacing = 2

    # when true, chooses random rgb values between 0.5 and 1.0. alpha is set to 0.5
    random_colors = true

    # set this to the desired background color if enable_range_colors is false
    default_background_color = [0.75, 0.0, 0.75, 0.5]

    # adds column for additional rider/driver notes
    notes_column = true

    # column spacing
    column_buffer_left = 0


